# Python SMTP Mail Client
This script uses the Simple Mail Transfer Protocol.
It is defaulted to login and send at Gmail's SMTP server: smtp.gmail.com
Also defaulted on sending to the server's port 587